export var config = {
  token: '',
}

// try {
  
//   let custom = await import('/widgets/custom.js');
//   if (custom.config != null) {
//       config = merge(config, custom.config);
//   }

// } catch (err) {
//   console.warn(err);
// } finally {
//   console.log(config);
// }